name: F_BLUEPRINT_DRIFT
description: "Detect drift between merged PRs and blueprint.yaml state across the metafactory ecosystem"
source:
  type: inline
destination:
  type: stdout
pipeline: ./pipeline.yaml
input:
  org: "the-metafactory"
  limit: 200
  sinceDate: "2026-01-01"
  outputPath: "/tmp/blueprint-drift.md"
