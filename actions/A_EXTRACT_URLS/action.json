{
  "name": "A_EXTRACT_URLS",
  "version": "1.0.0",
  "description": "Extract and deduplicate HTTP(S) URLs from text, stripping tracking wrappers",
  "input": {
    "emailBody": { "type": "string", "required": true }
  },
  "output": {
    "urls": { "type": "array" },
    "urlCount": { "type": "number" }
  },
  "requires": [],
  "tags": ["extract", "urls", "parse"]
}
