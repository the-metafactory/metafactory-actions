{
  "name": "A_CROSSREF_SIFT",
  "version": "1.0.0",
  "description": "Cross-reference extracted URLs against existing sift sources, return only new candidates",
  "input": {
    "urls": { "type": "array", "required": true }
  },
  "output": {
    "newCandidates": { "type": "array" },
    "alreadyKnown": { "type": "number" }
  },
  "requires": ["shell"],
  "tags": ["sift", "crossref", "dedup"]
}
