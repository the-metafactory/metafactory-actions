{
  "name": "A_EVALUATE_CANDIDATES",
  "version": "1.0.0",
  "description": "Use LLM to evaluate URL candidates — classify as blog/news (worth following) vs one-off content",
  "input": {
    "newCandidates": { "type": "array", "required": true }
  },
  "output": {
    "approved": { "type": "array" },
    "rejected": { "type": "array" }
  },
  "requires": ["llm"],
  "tags": ["evaluate", "llm", "classify", "sift"]
}
